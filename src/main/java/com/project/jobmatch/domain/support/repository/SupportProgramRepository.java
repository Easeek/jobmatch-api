package com.project.jobmatch.domain.support.repository;

import com.project.jobmatch.domain.support.entity.SupportProgram;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SupportProgramRepository extends JpaRepository<SupportProgram, Long> {
}
